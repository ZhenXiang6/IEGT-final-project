# OTT 動態策略模型－參數實驗報告

> 目的:在不改動模型結構的前提下,系統性掃描各外生參數,觀察**最適策略區間、均衡(穩態)、與在位者優勢**如何改變,並從中萃取**非顯而易見**的策略啟示——而不是「某策略成本變高所以變差」這種顯然結論。
>
> 工具:`experiments.py`(可重現的掃描 harness;value iteration,格點 151/401/801,收斂門檻 1e-7)。所有數字皆由程式輸出,未經手調。本報告先不寫進簡報。

---

## 摘要:六個值得寫進結論的啟示

1. **三段策略結構是「不變量」。** 在 11 個參數、各約 6–7 個值的測試範圍內,最適策略**恆為 `T-A → S-A → S-R`**(試水 → 衝量 → 收割)。任何參數都只\*\*移動切換門檻\*\*,從不改變階段的「順序」或「數目」。模型給的是一套**穩健的平台生命週期劇本**,校準只決定「何時換檔」。
2. **2×2 決策幾乎正交(維度解耦)。** 「定價維度」(TVOD→SVOD)的門檻幾乎只由**價格比 $p_S/p_T$ 與耐心 $\beta$** 決定;「客群維度」(獲取→留存)的門檻幾乎只由**成本規模 $\lambda_A$、留存效果 $\delta_R$、留存外部性 $\kappa_\rho$** 決定。兩條 1 維門檻各由不同的經濟力量驅動。
3. **耐心悖論:先行者優勢是「短視」的產物。** $\beta$ 越大,在位者優勢反而**越小**(0.75 → 0.15)。先行者的領先只是一筆「過渡期年金」;當你越看重共同的長期未來,這筆領先的折現相對價值就越小。
4. **成本悖論:把獲取變貴反而「鞏固」在位者。** 提高**規模相依的獲取成本** $\lambda_A$,穩態市佔**完全不變**(目的地一樣),但在位者優勢**上升**——因為它是一道**進入障礙**,專門懲罰想往上爬的小挑戰者。留存成本 $\lambda_R$ 則方向相反。
5. **招牌的「TVOD 隨受眾放大」($\varphi_1$)在均衡下幾乎無作用。** 把 $\varphi_1$ 從 0 拉到 1.5,策略地圖、穩態、在位者優勢**幾乎零變化**——因為廠商在很低的 $s$ 就已放棄 TVOD,規模效應還來不及發酵。一個「看似重要、實則不 binding」的特徵。
6. **「網路效應」不是單一方向的力。** 留存端外部性 $\kappa_\rho$ **鞏固龍頭**(穩態跳升、市場集中、甚至出現雙穩態/路徑相依);獲取端外部性 $\kappa_\alpha$ 卻**幫助挑戰者追趕**。同樣叫「網路效應」,作用在留存或獲取,競爭後果完全相反。

---

## 基準情境(baseline,n=401)

| 項目 | 值 |
|---|---|
| 策略區間 | `T-A` $[0,0.107]$ → `S-A` $[0.11,0.535]$ → `S-R` $[0.537,1]$ |
| 定價切換門檻(T→S) | $s\approx 0.11$ |
| 客群切換門檻(A→R) | $s\approx 0.54$ |
| 穩態 | $s_\infty\approx 0.744$(唯一、全域穩定) |
| 在位者優勢 | $\approx 0.23$(以 $s_0{=}0.65$ vs $0.05$ 的折現利潤差衡量) |
| 收斂速度 | 小型新進者約 **8 期**達 $s_\infty$ 的 90% |

> $(T,R)$ 在任何 $s$ 都不是最適——TVOD 人均收入低,不值得投入昂貴的留存。所以策略空間實質只剩三格。

---

## 發現 1:三段結構的「不變性」

掃描 $\beta, \kappa_\rho, \kappa_\alpha, \lambda_A, \lambda_R, p_T, p_S, \varphi_1, \rho_0, \delta_R, \alpha_0$ 共 11 個參數,**每一次**輸出的 cell 序列都是 `T-A → S-A → S-R`(程式以 "TSS" 表示定價序列 T,S,S)。沒有任何測試值讓某一段消失、或讓順序翻轉。

**啟示**:模型的「定性結論」對校準極為穩健。報告可以理直氣壯地說——這套「初創期低價試水 → 成長期訂閱衝量 → 成熟期訂閱留客」的生命週期,**不是某組參數湊出來的巧合**,而是結構性的;爭論具體門檻數值(0.11、0.54)意義不大,真正穩健的是「三段、且這個順序」。

---

## 發現 2:維度解耦(dimension separation)

把兩個切換門檻拆開看,它們各自只對一組參數敏感:

**定價門檻(T→S)** — 只被「收入面」參數推動:

| 參數 | 變化 | T→S 門檻 |
|---|---|---|
| $p_T$(TVOD 價) | 3 → 10 | 0.107 → 0.133(TVOD 越值錢,越晚放棄它) |
| $p_S$(SVOD 價) | 8 → 22 | 0.133 → 0.107(SVOD 越值錢,越早轉它) |
| $\lambda_A$(獲取成本規模) | 0 → 3 | **幾乎不動**(0.107–0.127) |

**客群門檻(A→R)** — 只被「成本/留存」參數推動:

| 參數 | 變化 | A→R 門檻 |
|---|---|---|
| $\lambda_A$(獲取成本規模) | 0 → 3 | 0.587 → 0.473(獲取越貴,越早轉留存) |
| $\delta_R$(留存策略效果) | 0 → 0.2 | 0.733 → 0.40(留存越有效,越早轉) |
| $p_T$(TVOD 價) | 3 → 10 | **完全不動**(0.54) |

**啟示**:這個 2×2 模型在均衡層面**退化成兩個近乎獨立的一維問題**。對 OTT 平台的實務意義——「要不要從單片轉訂閱」由**定價/收入結構**決定;「要不要從拉新轉留存」由**成本與留存科技**決定,兩者可以分開診斷、分開下決策,不必綁在一起想。

---

## 發現 3:耐心悖論——先行者優勢隨耐心「縮小」

| $\beta$ | T→S | A→R | 在位者優勢 |
|---|---|---|---|
| 0.80 | 0.080 | 0.500 | **0.752** |
| 0.85 | 0.087 | 0.513 | 0.536 |
| 0.90 | 0.100 | 0.527 | 0.363 |
| 0.95 | 0.113 | 0.540 | 0.230 |
| 0.99 | 0.127 | 0.547 | **0.153** |

兩個現象同時發生:

- **先行者優勢隨 $\beta$ 單調下降**(0.75 → 0.15)。所有廠商最終都收斂到同一個 $s_\infty\approx0.744$;大、小廠的差別只在「過渡期」。$\beta$ 越大,越看重那個人人平等的長期未來,過渡期的領先折現後就越不值錢。**先行者優勢本質上是一種短視(myopia)的度量**——它在廠商越急功近利時越被放大。
- **耐心同時把兩個門檻往後推**(T→S 0.08→0.13、A→R 0.50→0.55)。有耐心的廠商**把「成長/圈地」階段拉得更長**:更晚漲價、更晚轉留存,因為它更看重未來市佔的成長。

**啟示**:報告談「在位者優勢」時不能只說「大廠贏」;要說清楚——**這個優勢在參與者越短視時越大,在越有耐心的產業裡越小**。對快速折舊(高度競爭、資本沒耐心)的串流戰場,先行者其實享有可觀但**脆弱**的領先。

---

## 發現 4:成本悖論——獲取與留存成本對「誰贏」的影響相反

關鍵對照:**改變成本規模,穩態目的地 $s_\infty$ 一動也不動(恆為 0.7444),但「誰更容易到達」會變。**

| $\lambda_A$(獲取成本規模) | A→R 門檻 | 穩態 | 在位者優勢 |
|---|---|---|---|
| 0.0 | 0.587 | 0.7444 | 0.217 |
| 1.0 | 0.540 | 0.7444 | 0.230 |
| 2.0 | 0.500 | 0.7444 | 0.240 |
| 3.0 | 0.473 | 0.7444 | **0.251** |

| $\lambda_R$(留存成本規模) | A→R 門檻 | 穩態 | 在位者優勢 |
|---|---|---|---|
| 0.0 | 0.533 | 0.7444 | 0.231 |
| 1.0 | 0.560 | 0.7444 | 0.227 |
| 2.0 | 0.593 | 0.7444 | **0.219** |

- **獲取成本隨規模上升($\lambda_A\uparrow$)→ 在位者優勢上升。** 這是一道**進入障礙**:小挑戰者要往上爬,必須一路付越來越貴的拉新成本;在位者早已坐在高 $s$、改吃便宜的留存。成本「結構」沒改變終點,卻改變了**誰覺得這條路便宜**。
- **留存成本隨規模上升($\lambda_R\uparrow$)→ 在位者優勢下降。** 因為高 $s$ 的在位者才重度依賴留存,這個成本專門打它。

對照組——**基礎獲取難度** $\alpha_0$:

| $\alpha_0$ | 穩態 | 在位者優勢 |
|---|---|---|
| 0.04(難拉新) | 0.643 | **0.325** |
| 0.10 | 0.744 | 0.230 |
| 0.20(易拉新) | 0.821 | **0.152** |

**啟示**:**拉新越容易、越「規模中立」,市場越平等;拉新越貴、越「規模懲罰大廠以外的人」,在位者越穩固。** 對監理或挑戰者而言,真正鬆動龍頭的槓桿不是「打價格」,而是**降低規模相依的獲取成本**(例如共用的內容發行管道、跨平台拉新基礎設施)。

---

## 發現 5:被高估的特徵——TVOD 規模效應 $\varphi_1$ 幾乎無感

收入函數 $R(s,T)=p_T(\varphi_0+\varphi_1 s)$ 的招牌設定是「平台越大、TVOD 人均購買越多」。但掃描 $\varphi_1$:

| $\varphi_1$ | T→S | A→R | 穩態 | 在位者優勢 |
|---|---|---|---|---|
| 0.0 | 0.113 | 0.54 | 0.7444 | 0.2303 |
| 0.5 | 0.113 | 0.54 | 0.7444 | 0.2303 |
| 1.5 | 0.113 | 0.54 | 0.7444 | 0.2302 |

**幾乎完全沒有反應。** 機制:廠商在 $s\approx0.11$ 就已從 TVOD 切到 SVOD;在 TVOD 仍被使用的低 $s$ 區,$\varphi_1 s$ 這一項小到不影響決策,而切換之後 $\varphi_1$ 根本不再進入。

**啟示**:一個「看起來很重要、寫進模型也很合理」的特徵,在**均衡下可能完全不 binding**。報告值得誠實點出這點——它反而強化了「定價門檻由 $p_S$ vs $p_T$ 的水準、而非 TVOD 的規模斜率決定」的結論。也提醒:模型的複雜度不等於解釋力。

---

## 發現 6:留存網路外部性 → 臨界跳躍與市場集中

固定其他參數,只調留存外部性 $\kappa_\rho$(高解析度 n=801,觀察不動點與其斜率):

| $\kappa_\rho$ | 穩態 $s^*$ | 不動點斜率 |
|---|---|---|
| 0.06 | 0.700 | 0.759 |
| 0.09 | 0.771 | 0.785 |
| **0.10** | **(過渡帶,無內部穩定點)** | $\to 1$ |
| 0.11 | 0.833 | 0.805 |
| 0.13 | 0.916 | 0.827 |
| ≥0.14 | 0.935(撞上 $\rho_{\max}$ 天花板) | — |

穩態在 $\kappa_\rho\approx0.10$ 附近**快速從 ~0.77 躍升到 ~0.93**,不動點斜率逼近 1(near-fold / 鞍結分岔的特徵),之後因留存率撞上 $\rho_{\max}$ 上限而**釘死在 ~0.935**——再強的外部性也無法突破(飽和)。

**啟示**:留存網路效應越強,市場越**傾向 winner-take-most**;且存在一個**臨界區**,跨過後龍頭幾乎獨佔。對應 Rohlfs(1974)臨界質量、Arthur(1989)lock-in。這比「穩態隨 $\kappa_\rho$ 平滑上升」更有故事性——它是一個**接近不連續的躍遷**。

---

## 發現 7:路徑相依——同樣的參數,起點決定命運(已驗證)

baseline 校準下,模型是**全域收斂**的(任何起點 → 同一個 $s_\infty$)。但這不是模型的必然性質。當「留存外部性夠強 + 基礎留存偏弱 + 基礎獲取偏低」時,出現**兩個穩定內部均衡**,中間夾一個不穩定門檻:

| 情境 | 穩定低點 | 不穩定門檻 | 穩定高點 |
|---|---|---|---|
| $\rho_0{=}0.40,\kappa_\rho{=}0.50,\alpha_0{=}0.02$ | 0.574 | 0.607 | 0.910 |
| $\rho_0{=}0.45,\kappa_\rho{=}0.40,\alpha_0{=}0.02$ | 0.549 | 0.677 | 0.910 |

**模擬驗證(n=801,情境一,依起點 $s_0$ 的最終穩態):**

| $s_0$ | 0.02 | 0.10 | 0.30 | 0.50 | 0.70 | 0.98 |
|---|---|---|---|---|---|---|
| $s_\infty$ | 0.575 | 0.570 | 0.573 | 0.575 | **0.910** | **0.910** |

起點 $s_0\le0.5$ 的廠商困在 ~0.57 的「利基」均衡;起點 $s_0\ge0.7$ 的廠商衝上 ~0.91 的「主導」均衡。**完全相同的成本、價格、外部性,只因進場時的市佔不同,長期命運就分岔。**

**啟示**:這是比「先行者優勢」更強的論述——在強留存網路效應的產業,**歷史(起點)本身就是基本面**。早期搶到臨界門檻以上的玩家鎖定主導地位,落在門檻下的只能當利基。這替「為什麼要在串流早期不計成本搶市佔」提供了嚴格的動態理由。

---

## 發現 8:兩種網路外部性方向相反(已用於簡報第 17 頁)

| $\kappa_\alpha$(獲取端外部性) | 穩態 $s^*$ | 在位者優勢 |
|---|---|---|
| 0.0 | 0.524 | **0.280** |
| 0.2 | 0.744 | 0.230 |
| 0.4 | 0.851 | 0.187 |
| 0.6 | 0.897 | **0.160** |

- 留存端 $\kappa_\rho$:**鞏固既有者**(穩態升、市場集中、可致雙穩態)。
- 獲取端 $\kappa_\alpha$:**幫助挑戰者**(在位者優勢 0.28 → 0.16)。獲取流入 $\alpha(s)(1-s)$ 隨 $s$ 遞減(潛在客群枯竭),所以提升人均獲取率對「還小」的挑戰者幫助更大。

**啟示**:不要把「網路效應」當成單一變數。**它作用在留存還是獲取,對市場結構的後果是相反的。** 這也讓「網路效應 ⇒ 必然壟斷」的直覺需要修正。

---

## 發現 9:其他可寫進敏感度章節的穩健事實

- **收斂快且單調**:catch-up 時間在 $\beta$、$\lambda_A$ 變動下都約 **8 期**;拉新越容易則更快(高 $\alpha_0$ 約 5 期)。動態調整不是「慢慢逼近」,而是幾期內就到位。
- **基礎留存 $\rho_0$ 是穩態的主控鈕**:$\rho_0$ 0.5 → 0.8,$s_\infty$ 0.48 → 0.93(高 $\rho_0$ 同樣撞 $\rho_{\max}$ 釘死),且 A→R 門檻被推到很晚(0.77)——基礎留存夠高時,不急著靠「留存策略」加碼。
- **留存策略效果 $\delta_R$ 同時控制「收割時點」與「市場大小」**:$\delta_R$ 0 → 0.2,A→R 門檻 0.733 → 0.40(大幅提前),$s_\infty$ 0.67(臨界)→ 0.93;$\delta_R{=}0$ 時內部穩定均衡甚至消失。

---

## 對「結論章」的具體建議

1. 主結論換句話說:**模型給的是一套穩健的策略「劇本」(三段、固定順序),校準只決定換檔時點**——別把論述重心放在某個門檻的精確數值。
2. 把「在位者優勢」重新定性:**它是脆弱的、短視的**(隨耐心縮小),真正鞏固龍頭的是**規模相依的獲取成本**與**留存端網路效應**,而非價格。
3. 升級「市場集中」論述:從「穩態隨 $\kappa_\rho$ 上升」升級為**臨界躍遷 + 路徑相依(雙穩態)**——歷史起點決定命運,為早期燒錢搶市佔提供動態正當性。
4. 誠實面:點出 **$\varphi_1$(TVOD 規模效應)在均衡下幾乎不 binding**,反而凸顯定價門檻由價格水準決定。

---

### 附:重現方式

```bash
source .venv/bin/activate
python -c "from experiments import *; import json; print(json.dumps(analyze(n=401),ensure_ascii=False,indent=1))"
python -c "from experiments import sweep1; [print(r) for r in sweep1('beta',[0.8,0.85,0.9,0.95,0.99])]"
python -c "from experiments import multiplicity_scan; import numpy as np; [print(r['val'],r['n_stable'],r['fps']) for r in multiplicity_scan('kappa_rho',np.arange(0,0.2,0.01))]"
```

所有數字出自 `experiments.py`;完整掃描批次見本報告各表。模型校準同 `06_enhanced_model.py`。
