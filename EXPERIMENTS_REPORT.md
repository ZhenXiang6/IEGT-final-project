# OTT 動態策略模型：實驗洞察報告

> 研究筆記 — 給團隊內部閱讀，尚未放進簡報。
> 所有數字皆出自對抗式驗證後的重跑（harness：`experiments.py`，增強模型校準同 `06_enhanced_model.py`）。
> 模型核心：state = 市佔率 `s ∈ [0,1]`；action `(P,C) ∈ {T,S}×{A,R}`；
> 轉移 `s' = ρ(s)·s + α(s)·(1−s)`；`V(s)=max_a{π+βV(s')}`，value iteration。
> Baseline 校準（`DEFAULTS`）：`β=0.95, p_S=15, p_T=5, φ0=0.5, φ1=0.5, ρ0=0.65, δ_S=0.105, δ_R=0.10, κ_ρ=0.08, ρ_max=0.98, α0=0.10, t̃_T=0.05, t̃_A=0.15, κ_α=0.20, c_A=1.2, c_R=0.8, λ_A=1.0, λ_R=0.3`。
> Baseline 結果：策略地圖 `TA → SA → SR`，穩態 `s*≈0.744`，incumbent advantage ≈ 23%。

---

## Executive Summary

六個最具啟發性的核心洞察（一句話 + 關鍵數字）：

- **「耐心侵蝕在位優勢」是一個分母假象。** 提高 β（0.80→0.999）讓 incumbent advantage 的「比值」從 75% 崩到 14%，但在位者的**絕對折現美元先發紅利反而略升**（20.17→29.96）；25 年「未折現」每期利潤差總和在所有 β 下恆為 30.037。護城河沒變淺，是整個生命週期蛋糕被耐心放大、稀釋了比值。→ **衡量在位優勢請用絕對 gap，不要用 ratio。**

- **長期穩態 s*=0.744 對耐心「完全免疫」——只要 Bass 口碑（κ_α）開著、穩態落在 SR 區間內部。** β 從 0.5 掃到 0.9999，`stable_states=[0.7444]`、`n_stable=1` 逐字不變（n=101~1601 一致）；關掉 Bass（κ_α=0）後穩態被釘在會隨 β 移動的 A→R 政策門檻上（0.496→0.530）。**決定「耐心是否影響長期市佔」的不是耐心，而是不動點落在 regime 內部還是政策邊界上。**

- **口碑（κ_α）是挑戰者的朋友，留存外部性（κ_ρ）是在位者的朋友——兩股「都把 s* 推高」的力量對追趕速度方向相反。** κ_α 0→0.6 把 incumbent advantage 從 0.280 單調壓到 0.160（−43%，grid/horizon/14 組鄰近校準全穩健）；κ_ρ 反向把 inc_adv 推高（0.220→0.234）。機制：口碑乘在「未獲取池 (1−s)」上，落後者池大、吃滿口碑。

- **incumbent advantage 對留存外部性 κ_ρ 是非單調的——強到一定程度反而幫落後者追上。** inc_adv 隨 κ_ρ 升到 κ_ρ≈0.14 的峰值 0.2414 後**反轉下降**至 0.2029（κ_ρ=0.30）；**絕對 gap 也同樣峰值 32.94 後縮到 29.48**（真收斂，非分母假象）。深層驅動是 s≤1 的市佔飽和邊界把領先者邊際收益壓平，ρ_max 的硬上限只是強化並定位峰值。

- **獲取成本 c_A 是純粹的「進入稅」：只懲罰後進者，對在位者價值逐位數零影響。** c_A 0.6→12（甚至 100），在位者（s=0.65）25 年折現利潤 bit-for-bit 恆為 141.7905…（它永遠在 SR cell、從不付 c_A）；後進者利潤單調掉 118.0→101.4，inc_adv 0.20→0.40（近乎翻倍）。c_A 夠高時策略地圖長出基準從未出現的 **TR cell**，最終 TA 消失。

- **一小步留存成本（c_R 2.40→2.41）會觸發鞍結分岔式的穩態不連續崩塌（−10.4%）。** A→R 切換點被推過 SA 純行動吸引點 0.6667，低市佔進入者被永久困在 0.6667（而非 0.7444）。**反直覺反轉**：此時再「加一種成本」——調高獲取成本 c_A——竟能把切換點拉回 0.6667 以下、把廠商從陷阱救出來。

---

## 1. 耐心 β 與在位優勢 / 早期市佔的價值

### 1.1 「耐心侵蝕在位優勢」是分母假象 ✅ confirmed

**結論。** harness 直接回報的 `incumbent_adv` 是比值 `(disc[0.65]−disc[0.05])/|disc[0.05]|`，它隨耐心單調下降的線索會誤導人下「耐心讓後進者追得上、護城河變淺」的政策結論。拆成分子/分母後是**相反**的：護城河（絕對美元先發紅利）其實略**加深**。

**支持數字（baseline，n=401）。**

| β | inc_adv（比值） | 絕對 gap = disc[0.65]−disc[0.05] | 分母 disc[0.05] |
|---|---|---|---|
| 0.80 | 0.752 | 20.17 | 26.83 |
| 0.90 | 0.363 | 23.90 | — |
| 0.95 | 0.230 | 26.54 | — |
| 0.99 | 0.153 | 29.25 | — |
| 0.999 | 0.139 | 29.96 | 215.32 |

- 比值單調**下降**，但絕對 gap 單調**上升**。
- 關鍵不變量：25 年「**未折現**」每期利潤差總和在所有 β 下恰為 **30.037**；前 5 期每期在位者領先 `diff = [9.923, 5.775, 4.07, 2.142, 1.724]` 在所有 β 下逐字相同。
- 在 κ_α=0.6 下型態相同：gap 19.9→25.4（升），disc[0.05] 35.8→274.5（暴增），inc_adv 0.557→0.093。
- n=201 與 n=401 結果一致（非網格假象）。

**經濟機制。** 兩個起點（s0=0.05 vs 0.65）最終收斂到同一穩態 s*=0.7444，收斂後利潤流完全相同；差別只在前約 10–16 期的「暫態」。這筆暫態紅利的實質大小（未折現 30.037）與 β 無關。提高 β 只是把後段（對兩者相同）的價值放大進折現總和，所以分母 disc[0.05] 隨 β 近似 `1/(1−β)` 放大，固定的暫態分子只小幅變動 → 比值機械性下降。

**這啟發了什麼。** 這是一個 metric trap：分母發散、分子有界，比值必然下降。**任何「incumbent advantage 隨 X 下降」的宣稱，都要先檢查是不是分母在動。** 報告與簡報凡涉及在位優勢，應同時揭示絕對 gap，否則會得到與真相相反的策略結論。

### 1.2 穩態 s*=0.744 對耐心免疫——只要 Bass 開著且穩態在 regime 內部 ✅ confirmed

**結論。** 一般直覺是「更耐心 → 願意投資成長 → 達到更高長期市佔」。本模型恰恰相反：在 baseline（κ_α=0.20），β 從 0.5 掃到 0.9999，穩態終點 s*=0.7444 逐字不變。決定「耐心是否影響長期市佔」的**不是耐心本身**，而是 regime-一致的不動點落在 regime 內部（免疫）還是被釘在政策邊界上（隨 β 移動）。

**支持數字。**
- κ_α=0.20，n=801：β ∈ {0.5, 0.6, 0.7, 0.75, 0.8, 0.95, 0.999, 0.9999} 全部 `stable=[0.7444]`、`n_stable=1`、`cells=TA/SA/SR`；同時 C-switch 隨 β 大幅移動 0.364→0.55，但 **s* 不動**。網格：n=101/201/401/801/1601 全 `s*=[0.7444]`。
- 機制分解：純 SR 轉移映射（其中不含 β）的不動點恰為 0.7444，且該點位在所有 β 下的 A→R 門檻之上 → 穩態鄰域永遠用 SR、f(s) 形狀與 β 無關。
- **關掉 Bass（κ_α=0），n=801**：純 SR 不動點降到 0.485，低於 SA→SR 邊界，系統到不了純 SR 靜止點，改停在「會隨 β 移動的 A→R 門檻」上：β=0.8/0.85/0.9/0.95/0.99 → s*=0.4962/0.5061/0.5159/0.5233/0.5295，各自 `s*−Csw` 誤差 ≤0.0007（釘在邊界）。
- 對抗測試：Bass 開、p_S 由 15 降到 6，s* 仍恆=0.7444（免疫穩健）；Bass 關但 κ_ρ 升到 0.30/0.40，s* 仍≈Csw 並隨 β 動（0.833/0.876/0.888）——確認是「內部 vs 邊界」而非 Bass 本身。

> ⚠️ **nuanced 修正（兩處需在引用時降溫）。**
> (1) 只有**終點 s*** 對所有 β 不變；原宣稱「整條實現路徑逐字相同」**過強**——前 6 期路徑 `[0.05,0.327,0.468,0.554,0.592,0.623]` 僅在 β≳0.75 成立，β=0.5 為 `[0.05,0.285,0.441,0.498,…]`、β=0.6~0.7 又另一條。
> (2) 宣稱的收斂期數 t=16/t=10 **無法以自然門檻重現**（實得約 29/23），屬門檻定義相依，非結論要件。

**經濟機制。** 長期市佔由轉移函數 `f(s)=ρs+α(1−s)` 與「穩態當地所選策略」決定，而非由折現率決定。只要穩態深處於 SR 區間內部（離門檻很遠），β 怎麼變都摸不到它；耐心只重塑「通往穩態的路」，不重塑「穩態本身」——除非穩態剛好坐在政策切換邊界上。Bass 口碑把穩態推進 regime 內部，等於替長期市佔「加上對耐心的免疫」。

**這啟發了什麼。** 一個真實的 value-iteration DP 結構：**當策略最佳反應的內部不動點本身不含 β，長期狀態就對折現率免疫。** 對 OTT 故事的意涵——只要產品有口碑擴散把廠商推進「留存內部區」，長期市佔由技術（ρ、α）決定，與管理層多在乎未來無關。耐心的價值體現在過渡期效率，不在終局規模。

### 1.3 耐心把整個獲客投資階段的右端門檻推遠、壓縮收割區 ⚠️ nuanced

**結論。** 更耐心的廠商在更高的市佔之前都還不肯切換到「收割/留存」：兩個維度的切換點同步右移。但「同步『拓寬』兩個投資區」的框架不精確（修正見下）。

**支持數字（n=1601）。**
- P-switch（T→S）：β=0.8/0.9/0.95/0.99/0.999 = **0.077/0.097/0.110/0.122/0.124**（窗口拓寬約 +61%）。
- C-switch（A→R）：**0.496/0.525/0.537/0.547/0.549**（單調上移）。
- 固定 s=0.52：β≤0.85→SR，β≥0.9→SA（耐心者推遲收割）。固定 s=0.10：β≤0.9→SA，β≥0.95→TA（耐心者寧用虧本 TVOD 搶客）。
- Primitives 佐證：s=0.10 時 `π_TA=−1.045 < π_SA=+0.18`，但 `s'_TA=0.354 > s'_SA=0.319`（TVOD 多獲 +0.034 市佔，因 t̃_T=0.05 > t̃_S=0）；s=0.52 時 `π_SR=6.875 > π_SA=5.976`，但 `s'_SA=0.584 > s'_SR=0.564`。整個 TA 區皆虧損（π_TA 從 −1.20 到 −1.00 全為負）→「虧本獲客窗口」名實相符。
- 穩健：n=201/401/801/1601 兩門檻皆單調上移；11 組鄰近擾動全部維持單調。

> ⚠️ **nuanced 修正。** 逐區分解顯示 β 只真正**拓寬了 TA（虧本 TVOD 獲客）區**（寬度 0.077→0.124，**+61%**），而 SA 區寬度幾乎不變（0.419→0.425，僅 **+1.4%**，整段平移而非撐大）。真正被壓縮的是 **SR 收割區**（0.504→0.451，**−11%**）。正確說法：耐心把「整個獲客投資階段 [0, C-switch]」的右端推遠，代價是收割區縮小，且拓寬全部來自 TA 區。
> **另一個強假設依賴：** TVOD 窗口的存在完全靠 `t̃_T > t̃_S`。把 t̃_T 設為 0，P-switch 立刻塌到 ~0.002 且對 β 不再敏感，TVOD 故事消失；但 C-switch 故事獨立於此仍成立。

**經濟機制。** 每個動作是「當期利潤 vs 轉移後市佔 s'」的權衡。獲客型動作（TA、SA）犧牲當期現金換更快的 s' 成長，報酬被 β 放大；收割型（SR）拿當期現金但成長較慢。β 上升抬高未來權重 → 權衡向「成長」傾斜 → 放棄收割的臨界市佔上移。

**這啟發了什麼。** 耐心廠商會主動選擇**負利潤的 TVOD 動作當 loss-leader** 純為搶獲客速度，且 β 越高虧得越久。建議論文措辭用「耐心把整個獲客投資階段的右端門檻推遠、壓縮收割區」取代「同步拓寬兩個投資區間」，並標註 TVOD 窗口是 `t̃_T>t̃_S` 強假設驅動。

### 1.4 門檻雖隨耐心移動，baseline 實現路徑卻對耐心免疫（取樣混疊） ⚠️ nuanced

**結論。** §1.3 的 C-門檻隨 β 在 0.496→0.552 漂移，但從 baseline 起點 s0=0.05 出發的實現路徑「一步跨過」整段門檻帶，使動作序列在 [0.8, 0.999] 區間內對 β 完全不敏感——這是離散 DP 的取樣混疊（aliasing）。

**支持數字（n=801）。**
- C-門檻帶 [0.496, 0.552]（β=0.8→0.999）。
- s0=0.05 路徑 `0.05, 0.327, 0.4678, 0.5536, 0.5919, 0.6232`：t=2 的 0.468 < 帶下緣，t=3 的 0.554 > 帶上緣 → 跨越；序列 `TA,SA,SA,SR,SR` 在 β=0.8/0.95/0.999 逐字相同。
- 帶內反例 s0=0.52：β=0.8 首動作 SR（`0.52,0.564,0.601`）vs β≥0.9 首動作 SA（`0.52,0.584,0.617`）→ 路徑分岔。

> ⚠️ **nuanced 修正（兩個限定詞嚴重承重）。**
> (1) 「所有 β」為**偽**——免疫只在 [0.8, 0.999] 窗口；β=0.7 門檻 0.462 已低於 t=2(0.468)、β=0.5 門檻降到 0.365 且序列變 `SA,SA,SR…`。
> (2) 「baseline 校準」是**刀鋒巧合**：t=3（0.554）與門檻上緣（0.552）僅差 ~0.002；12 個鄰近擾動中免疫僅 4 存活、8 破裂，凡擾動 κ_α/α_0（決定躍遷步長）兩方向都破壞免疫。
> grid n 不影響（n=51/101/201/801 皆 straddle），真正脆弱性在**校準維度**。

**經濟機制。** 離散時間 DP 的轉移是有限步長躍遷而非連續流動。policy 是 s 的階梯函數，門檻是階梯位置；實現軌跡只取樣若干離散點。當相鄰兩取樣點（0.468 與 0.554）恰好包夾整段隨 β 漂移的門檻帶，門檻落在「取樣盲區」，β 對門檻的影響無法被任何實現路徑觀測到。這也是 §1.1 前 5 期利潤差在所有 β 下逐字相同的根因。

**這啟發了什麼。** 機制物理正確且有教育價值（解釋了為何 §1.1、§1.2 都對 β 不變），但**不可包裝成普遍命題**。誠實版：此 aliasing 巧合僅成立於精確 baseline 與 [0.8,0.999]；微調獲取參數即讓 β 可觀測。論文若用，須明確標為「單點上的取樣混疊」。

---

## 2. 留存網路外部性 κ_ρ 與市場集中 / 臨界質量 / path dependence

### 2.1 留存上限 ρ_max（而非 κ_ρ）設定集中度天花板：一場乾淨的「接力棒」regime split ⚠️ nuanced

**結論。** 提高 κ_ρ 平滑推升 s*，但**只到留存上限 ρ_max 綁定為止**；過了那點 κ_ρ 完全掉出 SR-cell 不動點方程，s* 由 ρ_max **與獲取項**（α_0, κ_α）共同決定。兩參數在臨界前線兩側「完美替代」與「互不相干」交替。

**支持數字（κ_ρ × ρ_max 格點，n=601）。**
- 低 κ_ρ：整列對 ρ_max 平坦（κ_ρ=0.04 → s*=0.662、κ_ρ=0.08 → 0.744、κ_ρ=0.12 → 0.872，對所有 ρ_max ∈ {0.96,0.98,0.99,1.0} 一致）→ ρ_max 無關。
- 高 κ_ρ（≥0.16）：整欄對 κ_ρ 平坦（ρ_max=0.96/0.98/0.99 → s*=0.8728/0.9348/0.9670，對 κ_ρ ∈ {0.16,0.20,0.25,0.30,0.40} byte-identical）→ κ_ρ 無關。
- 解析確認：cap-bound SR 不動點 `(1−ρ_max)s=(0.10+0.20s)(1−s)` 根 0.8728/0.9348/0.9670，與 VI 吻合至 4 位。
- 綁定門檻 share = `(ρ_max−ρ_0−δ_S−δ_R)/κ_ρ = 0.125/κ_ρ`。
- Grid-independent：cap-bound s* 對 n=201…2001 完全相同。

> ⚠️ **nuanced 修正。** headline「pinned by ρ_max **alone**」與其自身機制段矛盾。把 κ_α 當自由參數重解 cap-bound 根：κ_α 0.20→0.30 使 s*（ρ_max=0.98）從 0.9348→**0.9506**（無任何 κ_ρ 依賴），α_0=0.05 也移動 s*。所以「κ_ρ drops out」成立，「ρ_max **alone**」**不成立**——s* 由 ρ_max 與獲取項**共同**決定。
> 另：ρ_max=1.0 的「s*=1.000」是邊界吸收極限（`stable_states=[]`、s_inf→~1），非與其他同類的內部不動點；「κ_ρ≥0.16 綁定」門檻是 baseline 校準特定值。

**經濟機制。** `ρ(s)=ρ_0+δ_S+δ_R+κ_ρ·s` 被 ρ_max 截斷。κ_ρ·s 還壓在上限下時，不動點隨 κ_ρ 移動；一旦 κ_ρ 大到 ρ 在廠商抵達不動點前就撞上 ρ_max，穩態的留存率恰為 ρ_max，κ_ρ 退出方程。

**這啟發了什麼。** **超過上限後，口碑「留存」（κ_ρ）被浪費，但口碑「獲取」（κ_α）仍能移動天花板。** 對 OTT：當留存已逼近物理極限（取消率趨零），再投資「讓老用戶更黏」邊際歸零，資源應轉向獲取側口碑。

### 2.2 真實雙穩態 / 臨界質量只出現在低留存市場：1 個百分點的起始差翻轉長期結局 ✅ confirmed

**結論。** baseline 校準對**每個 κ_ρ 都是單一穩態**（無 path dependence）；把 baseline 留存降到 ρ_0=0.3、κ_ρ 推進 ~0.51–0.58，才打開真正的雙穩態窗口：一個 ~0.53 的利基均衡與一個 ~0.935 的近壟斷，中間隔一個不穩定臨界質量。

**支持數字（ρ_0=0.3，其餘 DEFAULTS，n=601）。**
- κ_ρ=0.52 → 不動點 `[0.5152 stable, 0.8788 unstable, 0.9348 stable]`；κ_ρ=0.54 → `[0.5287 s, 0.789 u, 0.9348 s]`；κ_ρ=0.58 → `[0.5813 s, 0.6032 u, 0.9348 s]`（不穩定分離點塌進低穩定點 = saddle-node）；κ_ρ≥0.60 → 只剩 `[0.9348 s]`（低均衡被消滅，市場全傾）。
- κ_ρ=0.54 separatrix（bisection）= **0.78898**：s0=0.78 → s_inf=0.533；s0=0.79 → s_inf=0.9348。
- 雙穩態窗口在 ρ_0 ∈ [0.28, 0.32] 穩健，ρ_0=0.25/0.35 消失；grid n=201→2001 不動點幾乎不變（非網格假象）。
- 高均衡 0.9348 = ρ_max=0.98 留存上限（傾到飽和天花板）。
- 對照：baseline ρ_0=0.65 ground-truth——對每個 κ_ρ ∈ [0,0.8]，從 0.02 與 0.99 出發都收斂到同一處（κ_ρ=0.10 兩者皆 →0.80），故 `n_stable=1` 處處成立。

> ⚠️ **兩個 framing 限定（不影響 confirmed）。**
> (1) 現象本身（留存外部性 → 臨界質量 / lock-in）**不是新的**——它是專案 Extension 1（`04_extension1.py`，README §7，κ≈0.16 附近）的招牌結果；此處貢獻是在**增強模型**中重現它，並釘出它需要刻意的低留存（ρ_0~0.3）regime。
> (2) `s_inf=0.533` 是**漸近極限**（約 T=200 達到），但配對的「25 年折現利潤 147.97」是 T=25 截斷（T=25 時 share 仍 0.6265）——兩數各自正確，但句子混了兩個 horizon。

**經濟機制。** ρ_0 低時，小 s 留存弱、churn 主導 → 廠商停在利基；但 κ_ρ·s 使留存隨 share 陡升，越過臨界質量後 ρ(s)·s 複利壓過 churn → 衝向 ρ_max 近壟斷。f(s) 三次穿越 45° 線：低穩定盆地、不穩定 separatrix（臨界質量）、高穩定盆地。落在哪邊純由初始條件（歷史）決定。

**這啟發了什麼。** **多重穩態不是網路效應的免費後果**——在 shipped 的 Bass/飽和轉移 `s'=ρs+α(1−s)` 與 default 校準下，運動律是凹的、對任何 κ_ρ 都只有單一穩態。雙穩態是 (低 ρ_0, 高 κ_ρ) 的**聯合**性質。OTT 意涵：只有在「基礎黏著度差但口碑留存強」的市場，先發/補貼搶臨界質量才真的決定生死。

### 2.3 incumbent advantage 對留存外部性是**非單調**的——夠強的留存 spillover 反而幫落後者追上 ⚠️ nuanced

**結論。** 教科書讀法是「網路/留存外部性 entrench 領先者」，應使 inc_adv 隨 κ_ρ 單調上升。實際相反：inc_adv 升到峰值後**反轉下降**。

**支持數字（n=601，只改 κ_ρ）。**
- inc_adv = 0.2196(κ_ρ=0.00) → 0.2303(0.08) → **峰值 0.2414(0.14)** → 0.2329(0.16) → 0.2200(0.20) → 0.2029(0.30)。
- 峰值恰與 s* 首次撞上 0.9348 plateau 同步（cap binds at κ_ρ~0.135）。
- **絕對 gap（hi−lo，n=401）也反轉**：21.47(0.00) → 峰 32.94(0.14) → 31.31(0.20) → 29.48(0.30)——真收斂，非分母假象。
- 穩健：grid n=101/201/401/601 皆峰值@0.14；horizon T=25/40/60/100 皆峰@0.14；κ_α、β、5 組 anchor pairs 全部非單調。

> ⚠️ **nuanced 修正（兩處機制需校正）。**
> (1) 原宣稱「numerator 固定、denominator 上升」**誤導**——絕對 lead 不是固定，它先升後降，這反而讓 headline 結論**更強**（是真收斂）。
> (2) ρ_max cap **不是唯一驅動**：ρ_max=1.00（無綁定上限）非單調性仍在（峰@0.14，降到 0.2078@0.30）。深層驅動是 **s≤1 市佔飽和**把領先者在 s* 的成長壓平；ρ_max 只是強化並定位峰值（ρ_max=0.95→峰@0.10；0.98→0.14）。

**經濟機制。** 領先者已逼近 share 上限、s* 飽和，從更強 spillover 幾乎榨不出邊際收益；落後者仍在留存曲線上升段，κ_ρ·s 在每個中間 s 加速其成長，等於 pro-laggard 補貼。固定（或下降）的分子 + 上升的分母 → 比值下降。

**這啟發了什麼。** **「贏者全拿」直覺有上限。** 一旦領先者撞到飽和天花板，再強的留存外部性就變成補貼後進者。對 OTT：監管者擔心網路效應鎖死市場時，要注意這個 saturation-induced convergence——超強留存外部性在高度集中市場反而壓縮領先差距。

### 2.4 κ_ρ 移動 A→R 切換點；配上昂貴留存可整段刪除 SR cell ⚠️ nuanced

**結論。** κ_ρ 不只 scale 穩態，還移動策略地圖門檻：A→R 切換點隨 κ_ρ 漂移，配上昂貴的留存行動可使廠商完全不切到留存，把 `TA→SA→SR` 塌成 `TA→SA`。

**支持數字（n=601）。**
- A→R 切換 share = 0.522(κ_ρ=0.00) → 0.538(0.08) → 0.548(0.12/0.16) → **0.823(0.30)**；T→S 定價切換幾乎不動（0.095→0.13）。
- κ_ρ=0.30 配升高 c_R：A→R = 0.823(c_R=0.8) → 0.937(c_R=1.5) → **SR cell 消失**，`cells=['TA','SA']`（c_R=2.5）。
- 控制組 κ_ρ=0.0、c_R=2.5 仍保留 SR（切換@0.772）。
- Grid-invariant n=201…1201。

> ⚠️ **nuanced 修正（三處）。**
> (1) A→R 漂移對 κ_ρ **非單調**：0.522(0.0)→峰 0.552(0.14)→谷 0.533(0.22)→跳 0.823(0.30)，不是乾淨「drifts later」。
> (2) 戲劇性的 0.823 跳躍主要是 **ρ_max=0.98 cap 把 δ_R 留存溢價歸零**的結果，**非**原宣稱的 λ_R·s 成本機制——把 ρ_max 升到 1.0，A→R 切換從 0.823 塌回 0.658。
> (3) SR-deletion **不是** κ_ρ×c_R 獨有交互：高 c_R 單獨即可刪 SR（κ_ρ=0.0 時需 c_R≈3.5；κ_ρ=0.30 時降到 1.9）——κ_ρ 只在 cap-binding regime 大幅拉低 deletion 門檻。

**經濟機制。** A→R 比較「δ_R 留存獎勵（只在 R 取得）」對「R 比 A 多付的成本 c_R(1+λ_R·s)」。κ_ρ·s 在 A、R 兩側等量抬高基礎留存，**不增加 δ_R 溢價**，卻抬高評估比較的均衡 s（經 λ_R·s 漲 R 成本）；當 ρ 撞 ρ_max，δ_R 溢價直接歸零，R 變純加成本。

**這啟發了什麼。** 反直覺：更強留存外部性**不會**讓留存行動更早被觸發，反而推遲或刪除它——因為外部性是 action-agnostic 的公共留存，不增加「付費 R」的相對價值。

---

## 3. 口碑 / Bass κ_α 與挑戰者追趕

### 3.1 口碑外部性是挑戰者的朋友：κ_α 越強，先發者優勢越小（單調遞減） ⚠️ nuanced

**結論。** 直覺「更強網路效應放大領先者滾雪球 → inc_adv 上升」是**錯的**。κ_α 上升**單調縮小** incumbent advantage，因為口碑乘在「未獲取池 (1−s)」上，落後者池大、吃滿口碑。

**支持數字（n=401，只改 κ_α）。**
- inc_adv = 0.280(κ_α=0) → 0.230(0.20, baseline) → 0.187(0.40) → 0.160(0.60)，0→0.6 區間 **−43%**，細步嚴格遞減。
- 異常穩健：grid n=151/201/401/801 四位數相同；horizon T=25/50/100/300 同方向同幅度（非有限期假象）；14 組鄰近基線（β 0.90–0.99、成本、價格、κ_ρ、ρ_0、α_0、λ_A）全部維持單調遞減。
- 機制確認：低/高起點收斂到同一 s*（差 ≤0.0006），故 inc_adv 純為過渡期追趕速度差；落後/領先者獲取流入比 `α(s)(1−s)` 隨 κ_α 由 2.71 壓到 1.19。
- 口碑只縮小、**從不反轉**優勢（κ_α=2.0 仍 inc_adv=0.108>0）。

> ⚠️ **nuanced 修正（兩處）。**
> (1) 對照組 κ_ρ 的宣稱數字錯誤——宣稱 0→0.10 把 inc_adv 從 0.254 推到 0.291，**實測為 0.220→0.234**（方向對、留存外部性確實反向推高，但引用數字不對且 κ_ρ 高端略非單調）。
> (2) 機制措辭略不精確：領先者的口碑流入其實**也上升**（0.0875→0.224），效應是相對/比值壓縮（2.71→1.19）而非領先者完全吃不到。

**經濟機制。** `α(s)=α_0+t̃+κ_α·s`，口碑項 κ_α·s 被 (1−s) 加權：落後者 (1−s)≈0.95 吃滿，領先者 (1−s)≈0.25 吃不到。口碑把成長機會「重分配」給市佔低的一方，壓縮先發/後發折現利潤差。

**這啟發了什麼。** **口碑是均衡化力量、留存外部性是 entrenchment 力量**——與 κ_ρ 的反向對照給了這洞察實質內容。對 OTT 挑戰者：在病毒式擴散強的市場（高 κ_α）進場最有利；對在位者：口碑強的品類護城河天生較淺。

### 3.2 同穩態規模下口碑「收斂更快」——兩股外部性把 s* 推同一邊、對追趕速度反向 ⚠️ nuanced

**結論。** κ_α 與 κ_ρ 都把 s* 推高，但對「不動點處 f(s) 局部斜率（= 均值回歸 / 追趕速度）」方向**相反**：口碑加速收斂（利挑戰者），留存外部性減速收斂（鎖住落後者更久）。在配對穩態下比較才能剝離市場大小效應——這正是反直覺所在。

**支持數字。**
- κ_α 掃描（s*, slope, half-life）：0.10→(0.6342, 0.830, 3.71)、0.20→(0.7444, 0.776, 2.74)、0.40→(0.8514, 0.610, 1.40)、0.60→(0.8971, 0.422, 0.80)；斜率單調**下降**。
- κ_ρ 掃描：0.0→(0.6035, 0.714)、0.05→(0.6802, 0.751)、0.10→(0.8000, 0.795)；斜率**上升**。
- 配對對照（s*≈0.80）：κ_α=0.30 → s*=0.8107 slope=0.698 vs κ_ρ=0.10 → s*=0.800 slope=0.795（口碑收斂更快），inc_adv 0.204 vs 0.234。
- 分解：固定 κ_ρ 只升 κ_α，inc_adv 0.261→0.171（降）；固定 κ_α 只升 κ_ρ，inc_adv 0.220→0.237（升）——乾淨支持方向。
- 斜率值在 grid n=201/401/801/1601 完全相同（非網格假象）。

> ⚠️ **nuanced 修正（三處）。**
> (1) 原 claim 括號「多重穩態來自 κ_ρ 而非口碑」在**此 harness 增強模型**中**不成立**：κ_ρ 在此校準下全程也只有單一穩態（`n_stable=1`），只是把唯一不動點推到 0.9348 飽和天花板。多重穩態 / saddle-node 是舊版 `04_extension1.py`（不同 (1−s)² 飽和、無 Bass α）的性質——屬把不同模型的性質誤植。
> (2) 同步等量調高兩者的宣稱數字 0.237→0.173，**實測為 0.230→0.184**（方向對、量級略低）。
> (3) d≥0.05 後高/低起點都收斂到幾乎相同 s*（~0.93–0.96），殘餘 inc_adv 主要是 25 年折現的暫態先發優勢，而非穩態鎖定。

**經濟機制。** κ_ρ·s 進留存率 ρ → 正回饋鎖定使不動點變陡、偏離衰減慢；κ_α·s 進獲取 α 但被 (1−s) 加權，對高 s 邊際遞減，使 f(s) 在高 s 更平 → 不動點更穩、偏離衰減快。

**這啟發了什麼。** **同樣大的市場，經由口碑長成的比經由留存長成的更不黏、挑戰者更容易追上。** 對 OTT 的長期結構：成長來源（病毒擴散 vs 黏著鎖定）決定市場可競爭性，而不只是規模本身。

### 3.3 口碑越強，廠商反而「更早」放棄獲取改打留存——獲取區間被壓縮（搭便車效應） ✅ confirmed

**結論。** 直覺「獲取口碑變強 → 獲取更划算 → 廠商更積極打獲取」是錯的。κ_α 上升使 A→R 門檻**下移**、SA 獲取帶縮小；s=0.5 處最適行動甚至從 A 翻成 R。κ_ρ 則相反、撐寬獲取區間。

**支持數字（n=801）。**
- A→R 門檻：κ_α=0.05→0.547、0.20→0.537、0.40→0.515、0.60→0.491（κ_α≳0.04 後單調下降）；SA 帶寬 0.452→0.427→0.401→0.388 同步縮小。
- 對照 κ_ρ 0.00→0.05→0.10→0.15：A→R 門檻 0.521→0.531→0.542→0.550（撐寬，反向）。
- s=0.5 的 `Q(SA)−Q(SR)`：κ_α=0.0→+0.120、0.20→+0.600、0.50→+0.029、0.60→−0.095；翻負發生在 κ_α≈0.52–0.53。
- 搭便車管道：次期市佔**淨增量** `s'(SA)−s'(SR)` 在所有 κ_α 皆精確=**0.0250**（= alpha 優勢 0.075 − rho 留存優勢 δ_R·s=0.050）。
- 穩健：n=201/401/801/1601 收斂一致；在 t̃_A∈{0.10,0.15,0.20}、c_A∈{1.0,1.2,1.5}、β∈{0.90,0.95,0.97}、λ_A∈{0.5,1.0,1.5} 每組門檻都隨 κ_α 單調下降。

> ⚠️ **小修正。** 門檻對 κ_α 非全程單調——κ_α 0→0.04 先微升（0.524→0.546）後才下降，峰值在 0.04 附近而非 0.05；機制中「+0.025」指**淨**次期增量而非 alpha-inflow 差（後者為 0.075）。皆為描述精度問題，不影響方向與穩健性。

**經濟機制。** κ_α·s 是「公共成長」：不論選 A 或 R 都自動到帳，留存策略可**免費共享**。行動 A 只多買到固定 +0.025 淨次期市佔，卻要付 `c_A(1+λ_A s) > c_R(1+λ_R s)`。κ_α 上升把成長軌道整體推往高 s，V 在高 s 趨飽和（凹），固定 0.025 增量的折現價值縮水，先低於多付的獲取成本 → 門檻下移、Q 由正轉負。κ_ρ 放大的是「留存」而非可共享的獲取流，無此搭便車管道，反向撐寬。

**這啟發了什麼。** **強獲取口碑讓廠商搭便車、提早收手自掏腰包的獲取。** 對 OTT：病毒式品類裡，砸錢買用戶的最適窗口反而**更窄**——放手讓口碑做工、把預算轉向留存才是最適反應。

### 3.4 口碑的「利挑戰者」效果與 β 的交互——絕對量度假象 ⚠️ nuanced

**結論。** 原宣稱「沒耐心廠商的口碑敏感度約為耐心廠商 3.4 倍」是**絕對值量度的水平假象**，而非真實的 β×κ_α 交互。

**支持數字（n=401）。**
- 絕對降幅：β=0.85 drop=0.2669（0.659→0.392）；β=0.95 drop=0.1202（0.280→0.160）；β=0.99 drop=0.0785（0.181→0.103）；比值 3.40。
- **決定性反證**：相對降幅 `(a−c)/a` 隨 β 遞**增**而非遞減——β=0.80→0.99 為 39.2%→43.3%（κ_α 0→0.6）；若有趨勢，反而是耐心廠商被侵蝕得**略多**，與宣稱方向相反。
- 分解：β=0.85/0.99 的「基準水平比」3.63 ≈「絕對降幅比」3.40，相對敏感度比僅 0.94。
- 機制確認：每個 (β,κ_α) 組合 `n_stable=1`，三起點 0.05/0.30/0.65 的 s_inf 完全相同。

> ⚠️ **verdict：nuanced（量度假象）。** 數字 100% 可重現且 grid/鄰近 β 穩健，但「洞察」把量度假象包裝成因果交互。當 β 低、折現重，過渡期利潤差的**絕對水平**天生較大，固定比例的侵蝕在絕對單位上自然看起來更大。改用相對幅度，β 的「交互放大」幾乎消失（且方向反轉）。

**這啟發了什麼。** 這是「沒耐心廠商先發優勢本來就大」這件顯而易見的事，被 κ_α 的絕對降幅**重新包裝**成「口碑對沒耐心廠商最致命」。**建議論文用相對降幅或直接報導 inc_adv 水平本身，避免誤導。** 與 §1.1 同屬一類 metric trap，值得在 Caveats 統一強調。

---

## 4. 成本結構（c_A, c_R, λ_A, λ_R）與策略切換、dimension separation

### 4.1 維度分離是不對稱的：收入參數守規則、成本參數會跨維度洩漏（靜態零、動態非零） ⚠️ nuanced

**結論。** 「revenue 只依 P、cost 只依 C → 完美 dimension separation」這個靜態直覺只對收費端的 p_T 成立。一個參數**守分離 iff 它在相關 action-pair 比較中相消**；管道是 Bellman continuation 項，不是「收入 vs 成本」本身。

**支持數字（n=801）。**
- p_T 3→10：T→S 切換 0.104→0.130（單調），A→R 切換**逐位數不變 = 0.537**（grid n=201/401/801/1601 皆 byte-for-byte）。
- c_A 0.6→5.0：A→R 切換 0.6→0.268（dC/dc_A≈−0.091/unit），**同時** T→S 切換 0.103→0.149（弱漂移 dP/dc_A≈+0.017/unit）。
- 靜態驗證：s≈0.11 的 `π_SA−π_TA=1.3447` 對 c_A 不變至 ~1e-16（TA、SA 同用 A，c_A 相消）。
- 動態驗證：V 在 [0.1,0.2] 斜率 c_A=1.2 時 39.0、c_A=5.0 時 43.8；`s'_TA=0.359 vs s'_SA=0.326`。

> ⚠️ **nuanced 修正（「revenue 守、cost 洩」二分法錯）。** p_S（純 SVOD 收入）**也**洩漏進兩維（P 0.118→0.107、C 0.51→0.552），靠動態-V 管道；c_R（純成本）也洩漏（P 0.11→0.09、C 0.496→0.838）。正確分類：**p_T、φ1（TVOD-only 收入）守分離，因其影響侷限在遠離 A→R 邊界的低 s TVOD regime；觸及高 s SVOD regime 的參數（p_S, c_A, c_R）都經共用 value function 跨維洩漏，不分收入或成本。**

**經濟機制。** T→S 切換由「靜態利潤差 + β·連續價值差」決定。c_A 在 TA/SA 兩側相消（靜態），但它改變最適未來路徑與 V 的形狀（高 c_A 使低市佔被困代價更大、V 在低 s 變陡 39→44），從而改變 `s'_TA` vs `s'_SA` 落點對應的連續價值差，經 Bellman 把成本「洩漏」進收費維度。這是 DP 特有的純動態耦合。

**這啟發了什麼。** 真正有趣且正確的部分是**DP-specific 的動態洩漏**：c_A 可證明對定價決策靜態中性，卻仍透過共用 value function 彎曲定價邊界。對模型故事：**dimension separation 在靜態模型成立，在動態 DP 不再乾淨**——這是引入動態的真實後果，論文應如實陳述，勿沿用靜態分離假設。

### 4.2 留存成本邊際上升觸發鞍結式穩態不連續崩塌（cost-induced path dependence） ⚠️ nuanced

**結論。** c_R 從 2.40 升到 2.41 這一小步，把 A→R 切換點推過 SA 純行動吸引點 0.6667，瞬間使低市佔進入者被困在 0.6667（而非 0.7444），穩態不連續掉 **−10.4%**。

**支持數字（n=801）。**
- 純行動不動點：SA=0.6667、SR=0.7444。
- c_R=2.40：A→R 切換 0.666、單一穩態 0.7444、s_inf(0.05)=0.742。
- True-dynamics basin test：c_R=2.40 run(0.05/0.60/0.70/0.72) 全 →0.74443；c_R=2.42 run(0.05/0.60)→0.66667、run(0.70/0.72)→0.74443（separatrix 在 0.60–0.70）；c_R=2.50 全 →0.66667。
- 參數穩健（true-dynamics bisection 的崩塌門檻 c_R*）：baseline 2.407（−10.4%）；c_A=1.0→2.134；c_A=1.4→2.681；β=0.93→2.351；p_S=14→2.356；κ_ρ=0.10→2.726（−14.1%）；κ_α=0.25→2.574（−11.7%）。

> ⚠️ **nuanced 修正（兩處）。**
> (1) 真正觸發點是 c_R ∈ (2.40, 2.41]，**非「2.40→2.42」**——崩塌在 2.41 已完成，2.42 是一分錢鬆。
> (2) 「三不動點 / `n_stable=2` 雙穩」標籤是 **grid-fragile**：n=201/401 把 0.6667 誤標 unstable（`n_stable=1`）、n=601 整個漏掉、僅 n≥801 讀為 stable。引用的 n=801 恰是 FP-classifier label 乾淨的網格。**雙穩態本身真實**（由 basin/trajectory 測試獨立確認），只有其 stable/unstable 形式標籤與精確 crossing 點（隨網格抖 ~0.006）對網格敏感。

**經濟機制。** baseline 能停在 0.744，因為廠商在 s=0.537(<0.667) 就切到 R、沿 SR 映射爬到 0.744。c_R 升高推 A→R 切換點上移；一旦越過 SA 自身吸引點 0.667，從低市佔出發者先被 SA 映射拉到 0.667 就停住，永遠達不到啟動留存所需市佔 → 鎖在 SA 的 0.667。切換點落在 0.667 附近時扮演 separatrix → 雙穩態與 path dependence。

**這啟發了什麼。** 反直覺：留存成本升高**不是**平滑小降，而是質變式不連續崩塌——崩塌不是因為留存變不划算（payoff-induced），而是因為它把「開始留存」的門檻推過一個**動態吸引點**（regime-induced）。對 OTT：成本微調可能引發長期市佔的斷崖式 lock-out，這是政策設計需警惕的非線性。

### 4.3 提高獲取成本（c_A 或 λ_A）反而把廠商從留存成本崩塌中救出——sign-reversing cost interaction ⚠️ nuanced

**結論。** 在會引發崩塌的 c_R=2.5 下，把獲取成本 c_A（或斜率 λ_A）調高，反而把系統從 0.667 救回 0.744——「再加一種成本」修復了另一種成本的崩塌。

**支持數字（n=601）。**
- c_R=2.5：c_A=1.2 → A→R 切換 0.745、s_inf(0.05)=0.6667（崩塌）；c_A=1.4 → 0.652、s_inf=0.7434（復原）；c_A=2.0 → 0.585、0.7437。
- λ_A 同向：c_R=2.5、λ_A=1.0→0.745/崩塌、λ_A=2.0→0.622/0.7437/復原。
- 救援邊界落在 c_A=1.26（0.6667）與 1.28（0.7425）之間。
- Transition cost-invariance 驗證：`transition(0.6,SA)` 對 (c_A=1.2,c_R=2.5) 與 (c_A=2.0,c_R=0.8) 相同 = 0.6298（成本不進運動律，只移政策門檻）。
- Grid-robust n=201…1201；robust to β∈{0.93,0.95,0.97}。

> ⚠️ **nuanced 修正（兩處）。**
> (1) 「`n_stable` 0→1」是 **grid-detection artifact**，非「無穩態」。崩塌的 c_A=1.2 case **有**穩定穩態——就是 0.6667 的 SA-trap（s_inf 收斂到那）；harness 只因 n=601 一格節點恰落 0.6667（嚴格 sign-change `<` 漏掉）才報 `n_stable=0`，n=602/801 同 case 報 `n_stable=1`。**救援是把吸引子從 0.6667 搬到 0.7444，不是無中生有造一個穩態。請用 s_inf 而非 n_stable。**
> (2) 效應活在 **κ_ρ 的窗口**：κ_ρ=0.05 無救援（兩 c_A 皆 0.6395，兩吸引子已合併），baseline 0.08 有效，0.12 廠商從不被困（兩者皆 0.867）。救援需要 low-trap 與 high-state 共存的雙穩 regime。

**經濟機制。** 成本不進運動律（對固定 action，轉移與成本無關），只移 A→R 政策門檻。c_R 升高使留存太貴 → 延後切換（門檻上移過 0.667 → 崩塌）；c_A/λ_A 升高使「繼續用 A」太貴 → 提早切換到 R（門檻下移）。兩成本對門檻方向**相反** → 提高 c_A 把門檻拉回 0.667 以下，重新讓低市佔廠商沿 SR 路徑爬到 0.744。

**這啟發了什麼。** 穩態崩塌由「A→R 切換點 vs 0.667 吸引點」的**相對位置**決定，而非由總利潤水準決定——所以一個把門檻往下推的參數能抵消另一個往上推的。這是 saddle-point 周圍 opposite-signed threshold shift 的非平凡反轉，提醒「兩種成本都升 → 更糟」的直覺在 regime-induced 動態裡會失效。

### 4.4 獲取成本是純進入障礙：嚴格壓低進入者價值、在位者價值逐位數不變，並生出新 TR cell ⚠️ nuanced

**結論。** c_A 只懲罰低市佔進入者，對高市佔在位者價值**完全無影響**（bit-for-bit），使 incumbent advantage 近乎倍增；c_A 夠高時策略地圖長出基準從未出現的 **TR（TVOD+留存）cell**，最終 TA 完全消失。

**支持數字（n=601/801）。**
- c_A 0.6→12（甚至 100）：在位者 disc(0.65) = **141.7905150527682** 逐位數相同（路徑每期皆 'SR'、`uses_any_A=False`）；進入者 disc(0.05) 單調掉 118.008→101.393；inc_adv 0.2015→0.3984（近乎翻倍）。
- 策略 cell：c_A≤7.0 為 TA-SA-SR；c_A≈7.45 出現 TR（TASASR→TATRSR）；c_A≈11.7 TA 消失（→TRSR）。
- V(0) 單調降 173.4→154.8；V(0.5) 在 c_A≥2 後鎖在 189.04。
- Cells grid-invariant n=201…1201。

> ⚠️ **nuanced 修正（兩個真實 nuance）。**
> (1) 「EXACTLY unchanged」**非普遍**——它是 baseline 幾何使在位者落在 retention-only regime 的性質。把 A→R 切換推過 0.65 即破：c_R=2.0→切換 0.804、disc(0.65) 掉 129.571→120.708；δ_A=0.2→156.860→141.791；ρ_0=0.4→83.848→64.939。
> (2) 進入者懲罰**不會無限增長**——disc(0.05)（101.393）與 inc_adv（0.3984）在 c_A≥11 **plateau**，因為夠高 c_A 下進入者也放棄獲取（路徑變 TR→SR）、自己也變 c_A-immune。
> 另：「new TR cell」指新於此 baseline 策略**地圖**——TR 是模型固定 action set 內的標準 action，非新發明的策略。

**經濟機制。** 在位者（s=0.65）已在 A→R 切換點之上、長期只用留存（付 c_R）、從不付 c_A → disc(0.65) 對 c_A 免疫。進入者必須先經低市佔 A 階段才能爬升，c_A 直接課在必經之路 → disc(0.05) 下降。c_A 極高時低市佔連用 A 都不划算 → 即便維持 TVOD 定價也改用留存（TR）；更高時開局即放棄獲取（TA 消失）。

**這啟發了什麼。** **c_A 是「只對爬升中後進者課稅」的不對稱進入稅，而非普遍營運成本**——同一參數對不同市佔廠商邊際效果天差地別。對 OTT：抬高獲取成本（如廣告投放變貴、CAC 上升）系統性偏袒在位者、加深護城河。且成本可以改變策略地圖的 **cell 種類**（生出 TR）而不只是門檻位置。

---

## 5. 策略空間邊界：每個 cell 何時被選（尤其 never-chosen TR cell）

### 5.1 TVOD 在市場頂端重新進場：最適定價政策對市佔非單調（T→S→T） ⚠️ nuanced

**結論。** 當 TVOD 收入強烈隨 reach 放大（φ1≳3.55），定價規則第二次反轉——低 s 用 TVOD、中段 SVOD、高 s 又回 TVOD——「高 share ⇒ SVOD」這個 baseline 不變量破裂，市場頂端變成 **TR**（baseline 從不選的 cell）。

**支持數字。**
- Baseline（φ1=0.5）：`TA[0,0.107]→SA[0.11,0.535]→SR[0.537,1.0]`，無 TR，單一 T→S@0.11。
- TR 首現 φ1≈3.55（n=801：3.50 缺、3.55 在 s≈1 出現 sliver）。
- φ1=4.0：`TA→SA→SR[0.537,0.878]→TR[0.88,1.0]`，**兩個** pricing switch：T→S@0.125、S→T@0.88。
- S→T 再進場點隨 φ1 單調下移：0.88(4.0)、0.812(4.3)、0.753(4.6)、0.718(5.0)、0.638(6.0)。
- Grid-robust n=201…1601；TR survives β∈[0.90,0.99]、φ0∈[0.3,0.7]、κ_ρ∈[0,0.2]；TR killed by p_T=4.0 與 δ_S=0.15。

> ⚠️ **nuanced 修正（機制錯 + reachability）。**
> (1) 原宣稱機制「TVOD 線第二次從下穿過 p_S」**數學錯誤**：`R(s,T)=p_T(φ0+φ1·s)` 嚴格遞增，與平的 p_S **恰交一次**（φ1=4.0 時 s_c=0.625）。真正驅動是**動態**：雖然 TVOD per-user 在 s>0.625 即勝，廠商在 0.625 之上一大段仍留 SVOD（S→T 在 0.88，~0.255 gap），因為 SVOD 留存嚴格較高（δ_S=0.105 vs δ_T=0）。SVOD 的留存溢價在中高段壓過收入差，TVOD 只在頂端 share 近吸收、留存劣勢幾乎不重要時才再進場。
> (2) **Off-path**：φ1=4.0 時 s*=0.744 < 再進場點 0.88，均衡路徑收斂到 SR、**從不實際跑 TR**——TR 只是 dead high-s 區。TR 成為真正長期政策需 φ1~7+（s* 本身落進 TR）。
> (3) φ1≳3.55 是 calibrated φ1=0.5 的 ~7 倍，這是 regime-existence 結果，非對校準模型的預測。

**經濟機制。** SVOD 收入平（p_S），TVOD 收入線性增。靜態 S-vs-T 唯一交點；但 SVOD 的 δ_S 留存優勢使它在收入交點之上仍最適，直到頂端 share 飽和。reborn-TVOD 落在 TR cell，因該區同時滿足「TVOD 勝 SVOD」與「高 s（已是 R 最適）」——只在陡 φ1 下共現。

**這啟發了什麼。** 反直覺：人們愛援引「大裝機量 → 偏好訂閱（可預測 ARPU）」，但**同一個力量（大 base）在 per-transaction 收入夠陡時讓交易型定價再次勝出**。對 OTT：若單次交易收入隨用戶規模強烈放大（如超級平台的交易抽成），最適定價是非單調的，霸主反而可能在頂端回到交易制。

### 5.2 陡 TVOD 收入下廠商刻意自限成長——但「pinned at switch」是 limit cycle 的誤分類 ⚠️ nuanced

**結論。** 一旦 φ1 高到 S→T 再進場點降到 ~0.744 附近，廠商自願放棄 share 去收割交易收入，**方向性結果真實**：raising φ1 把長期 share 拉下（0.744 @φ1≤4.6 → 低 0.60s @φ1=6.0），純為定價策略選擇（獲取/留存參數未動）。

**支持數字。**
- analyze 報的「stable FP」：0.740(φ1=4.7)、0.716(5.0)、0.675(5.5)、0.636(6.0)，與 S→T 切換點 track 到一格內。
- 但 per-action own-law 不動點（φ1=5.0）：SR=0.7444、**TR=0.5611**（TR 自己的不動點遠低於 SR）。
- 切換點 s=0.718 的 net inflow `f(s)−s`：SR +0.0058（仍成長）、**TR −0.0555**（急縮）——駁「switch 處 net inflow≈0」。

> ⚠️ **nuanced 修正（核心數字是誤分類）。** 廠商**不**把 share 釘在切換點。analyze 報的「stable fixed point」是 **misclassification**：S→T 切換處政策運動律有**向下不連續跳躍**（TR 不動點 ~0.56 遠低於 SR 0.744），切換處 TR net inflow 強負（~−0.055），FP-detector 把這個 discontinuity 的對角穿越誤標 stable（跨不連續的 local secant slope <1）。
> **真正長期行為是 robust limit cycle（週期 ~6）**：share 在 SR 爬到切換點（~0.72）、被一期 TVOD 打下（~0.66）、來回震盪。Cycle 在 n=401…4001 完全相同（cycle 本身非網格假象，但「單一 steady state」數字是）。實現長期**均值 share 比報的 s* 低 0.7–2.2 pts**（φ1=5.0：報 0.716、切換 0.718，真 cycle [0.664,0.721]、均值 0.695）。analyze 自己的 s_inf 已露餡：φ1=5.0 的 25 期端點 0.664/0.682/0.664（隨起點不同——取到不同 cycle 相位），從不是 0.716。

**經濟機制。** TVOD 缺 SVOD 留存獎勵 δ_S 與 SVOD 側 inflow，故其不動點較低。當定價在 s_switch <（SVOD 不動點）翻 T，廠商進入較低吸引子動態；但因 TR 不動點遠低於切換點，share 不是停在切換點而是被打下後再爬升，形成 limit cycle。

**這啟發了什麼。** 核心定性經濟（陡交易收入使廠商自願 flip 到 TVOD-Retention、提前放棄 share）成立；失敗在把 policy **discontinuity** 包裝成「pinned stable steady state」。**論文若用此結果，須說「長期是 limit cycle、實現均值在切換點之下」，不可說「均衡 share 被釘在切換點」。** 「within one grid cell」精確但 uninformative——兩量本是同一個 crossing。

### 5.3 Retain cell 可在策略地圖上、卻在均衡路徑外：高 c_R 先以「不可達」殺死留存、後以「次優」 ⚠️ nuanced

**結論。** 提高 c_R 不是直接讓 Retention 從 policy 消失；它**先**把 A→R 門檻推過穩態，廠商停在 Acquire 盆地、永遠到不了（仍是 argmax-if-you-got-there 的）Retain 區——SR cell 仍印在地圖上卻 behaviorally irrelevant。

**支持數字（n=601）。**
- c_R=0.8：A→R 切換 0.538、s_inf(0.65)=0.7442、cells=[TA,SA,SR]。
- c_R=2.0：切換 0.632、s_inf=0.7442（仍可達）。
- c_R=2.5：切換 **跳到 0.745**（已在 s* 之上）、s_inf **塌到 0.6667**、cells **仍** [TA,SA,SR]。
- c_R=3.0/3.5：切換 0.837/0.937、s_inf=0.6667、cells 仍 [TA,SA,SR]。
- 只有 c_R≈3.80 SR cell 才從地圖消失（cells=[TA,SA]、C_switches 空）。
- SA-only transition 不動點 = 0.66667；s0=0.65 在 c_R=2.5/3.0 的最適軌跡 max share=0.66667、on-path 只用 Acquire。
- Robust：n=201/401/601/1001 門檻一致；9 組鄰近校準（κ_α×κ_ρ）皆有一段 cells=[TA,SA,SR] 但 s_inf 塌到 SA-only 盆地不動點。

> ⚠️ **nuanced 修正。** (1) 被越過的是**較低的 SA 盆地不動點 ~0.667**，不是 s*=0.744。(2) SR 在 c_R≈3.80 消失（非 4.0）；崩塌 onset ~2.45（非 2.5）。on-map-but-unreachable 窗口為 c_R ∈ [~2.45, ~3.79]。(3) `n_stable` 在此 regime 不可靠（detector 漏掉 0.6667 吸引子），結論以 s_inf 為準。

**經濟機制。** A→R 邊界隨 c_R 上升。當門檻超過 Acquire-regime 自身不動點（0.667），受控軌跡收斂在 Acquire 區內、從不越進 Retain——即使 Retain 是門檻之上的 argmax，**沒有可達的 s 在門檻之上**。Retention 變 optimal-but-unreachable，是 basin / path-dependence 效應而非 pointwise dominance。

**這啟發了什麼。** **不能照字面讀策略地圖**——cell 在地圖上的存在與在均衡中的存在 decouple。一個小成本變化（c_R 2.0→2.5）能產生 ~10 點不連續穩態降，**且 cell inventory 不變**。對 OTT 分析：策略地圖必須疊上 reachable set，否則會誤判「廠商在高 share 會留存」。

### 5.4 便宜訂閱放大在位優勢（穩態不變）——profit-ratio 暫態效應，非 basin 效應 ⚠️ nuanced

**結論。** 降 p_S 約把 incumbent advantage 翻倍（0.23→0.45），儘管高/低 share 廠商收斂到**同一**穩態（~0.744）：優勢增長因為絕對利潤 base 縮得比固定早期 head-start gap 快、比值放大。

**支持數字（n=401，grid-invariant n=201…1601）。**
- p_S=15：disc_lo=115.25、disc_hi=141.79、inc_adv=0.230、ss=[0.7444]。
- p_S=8：54.27/69.06、inc=0.273。p_S=5：28.32/37.89、inc=0.338。
- p_S=3.5：15.44/22.31、inc=**0.445**、ss **仍** [0.7444]（穩態 15→3.5 全程 FLAT）。
- 成本/收入分解：p_S=15 lo cost=15.56、p_S=3.5 lo cost=14.76（cost 幾乎 p_S-invariant），revenue 隨 p_S scale → net disc_lo 縮更快；gap/p_S ≈ 1.77→1.96（roughly 常數）。
- Robustness：8 組鄰近參數 `monotone_up=True`（如 β=0.90: [0.363,0.44,0.557,0.672]；c_A=2.0: [0.262,0.322,0.412,0.485]）。

> ⚠️ **nuanced 修正（claim 後半 FALSE）。** **低 p_S 沒有 two-basin / basin-driven 機制**：p_S 15→1.0 全程 `n_stable=1`；p_S=3 時三起點 0.05/0.30/0.65 全收斂到同一 0.5611，優勢仍是單吸引子暫態效應。穩態**不是**在 p_S=3 跳變，而是從 p_S≈3.3 連續下滑（0.744→…→0.561）。且 0.23→0.45 幅度部分被 analyze 寫死的 25 期窗口放大（T=200 時降到 ~0.28，但定性 pattern 各 horizon 皆成立）。

**經濟機制。** 總折現利潤 ≈（穩態利潤）/(1−β) + 暫態。同穩態下 disc_hi−disc_lo 是落後者從 0.05 爬到 0.744 的暫態折現值——share 上大致固定但被 per-user 收入 scale。p_S 降，disc_lo 縮更快（落後者把早期重折現年份花在低 share 賺很少），gap/|disc_lo| 上升。

**這啟發了什麼。** 又一個 metric trap（與 §1.1、§3.4 同類）：**incumbent advantage 作為 ratio 對價格水準敏感，即使戰略情境（同吸引子）完全相同。** cost/revenue 分解給出真實、非顯然的機制，並正確預測 c_A 與 β 掃描方向；但 claim 自行發明的「low-p_S two-basin」不存在。建議報導 inc_adv 水平本身或固定 horizon。

---

## 6. 非單調與反直覺反轉（系統搜尋）

本節彙整全報告中已驗證的「非單調 / 反轉」清單，作為論文亮點的索引：

| # | 現象 | verdict | 關鍵反轉數字 |
|---|---|---|---|
| 1 | inc_adv 對 β：比值崩、絕對 gap 反升 | ✅ confirmed | ratio 0.752→0.139 vs gap 20.17→29.96（§1.1） |
| 2 | inc_adv 對 κ_ρ 非單調（峰後反轉） | ⚠️ nuanced | 峰 0.2414@κ_ρ=0.14 → 0.2029@0.30；絕對 gap 32.94→29.48（§2.3） |
| 3 | κ_α 強 → 廠商**更早**棄獲取（搭便車） | ✅ confirmed | A→R 門檻 0.547→0.491；s=0.5 Q 翻負 @κ_α≈0.52（§3.3） |
| 4 | 加 c_A 救援 c_R 崩塌（sign-reversing） | ⚠️ nuanced | c_R=2.5：c_A=1.2 塌 0.667、c_A=1.4 救回 0.743（§4.3） |
| 5 | c_A 抬高反而**擴大** inc_adv（進入稅） | ⚠️ nuanced | inc_adv 0.20→0.40、disc(0.65) 逐位數不變（§4.4） |
| 6 | 定價政策對 share 非單調 T→S→T | ⚠️ nuanced | φ1=4.0 兩切換 T→S@0.125、S→T@0.88（§5.1） |
| 7 | 陡 φ1 → 廠商自限成長（實為 limit cycle） | ⚠️ nuanced | s* 0.744→0.636；真 cycle 均值 0.695@φ1=5.0（§5.2） |

**統一主題：許多「驚人反轉」其實是 metric trap（§1.1, §3.4, §5.4）——比值的分母在動。** 真正結構性的非單調與反轉是 §2.3（飽和邊界）、§3.3（搭便車）、§4.2/§4.3（regime-induced saddle-node）。論文應把這兩類分開陳述。

---

## 7. Parameter → 行為 對照表

| 參數 ↑ | 門檻（A→R / T→S） | 穩態 s* | incumbent advantage | cell 出現 / 消失 | 驗證節 |
|---|---|---|---|---|---|
| **β（耐心）** | A→R、T→S 同步**右移**（0.496→0.549 / 0.077→0.124）；TA 區拓寬 +61%、SR 收割區 −11% | 內部時**免疫**（=0.744）；釘邊界時隨 β 移（κ_α=0：0.496→0.530） | 比值**下降**（0.75→0.14）但絕對 gap **上升**（20→30） | — | §1.1–1.4 |
| **κ_ρ（留存外部性）** | A→R **非單調**漂移（0.522→峰 0.552→0.823）；配高 c_R 刪 SR | 推高至 ρ_max-cap plateau（cap 後 κ_ρ 退出，s* 由 ρ_max+獲取項決定） | **非單調**：峰 0.2414@0.14 後反轉降 | 配高 c_R 刪 **SR**；低 ρ_0 開**雙穩態**（0.53/0.935） | §2.1–2.4 |
| **κ_α（口碑/Bass）** | A→R **下移**（0.547→0.491，搭便車）；獲取帶縮小 | 推高（0.634→0.897）；收斂斜率下降（追趕變快） | **單調下降**（0.280→0.160，−43%，極穩健） | s=0.5 行動 A→R 翻轉 @κ_α≈0.52 | §3.1–3.4 |
| **c_A（獲取成本）** | A→R **下移**（0.6→0.268，可救援 c_R 崩塌）；T→S 弱漂移（動態洩漏） | 不直接動（成本不進運動律）；可把崩塌救回 0.744 | **上升**（0.20→0.40）；在位者價值逐位數不變 | 高 c_A 生 **TR**（≈7.45）、TA 消失（≈11.7） | §4.1,4.3,4.4 |
| **c_R（留存成本）** | A→R **上移**（0.538→0.937）；越過 0.667 觸發崩塌 | (2.40,2.41] 不連續崩塌 **−10.4%**（0.744→0.667） | 隨崩塌降至 floor 0.190 | SR **on-map-but-unreachable**（c_R∈[2.45,3.79]）；c_R≈3.80 SR 消失 | §4.1,4.2,5.3 |
| **p_S（訂閱價）** | A→R、T→S 皆漂移（動態洩漏，破 dimension separation） | FLAT=0.744（p_S 15→3.5）；p_S≲3.3 連續滑落 | **上升**（0.23→0.45，profit-ratio 暫態，非 basin） | p_S<3 規模 TVOD takeover（TA→TR） | §4.1,5.4,1.2 |
| **p_T（交易價）** | 只動 T→S（0.104→0.130）；A→R **逐位數凍結**（守 dimension separation） | — | — | — | §4.1 |
| **φ1（TVOD reach 彈性）** | φ1≳3.55 第二定價反轉 S→T（0.88→0.638） | 高 φ1 自限成長（limit cycle，均值降至 0.62–0.70） | — | 頂端生 **TR**（off-path until φ1~7） | §5.1,5.2 |
| **ρ_max（留存上限）** | — | cap 後設集中度天花板（0.96/0.98/0.99→0.873/0.935/0.967） | 定位 §2.3 峰值位置 | — | §2.1,2.3 |
| **ρ_0（基礎留存）** | — | 低 ρ_0（~0.3）+高 κ_ρ 開**雙穩態**窗口 | — | — | §2.2 |

---

## 8. Caveats（網格假象、偵測器陷阱、模型限制）

### 8.1 已排除的網格假象 / FP-detector 陷阱

所有 headline 數字均跨多個網格解析度重跑確認，並把以下偵測器假象與真實現象區分清楚：

- **`n_stable` 不可靠於不連續政策切換處。** 多處（§4.2, §4.3, §5.3）harness 的嚴格 sign-change（`d[i]·d[i+1]<0`）測試會在 f(s)−s 的零點恰落網格節點、或 f(s) 在政策切換處跳躍時**漏掉真實吸引子**，回報 `n_stable=0` 或誤標 stable/unstable。**一律以 `s_inf`（直接 simulate 收斂值，跨網格穩健）為準，而非 `n_stable`。** 例：§4.3 同一 case n=601 報 0、n=602/801 報 1，而 s_inf 全程 0.6667。
- **「stable fixed point」可能是 limit cycle 的誤分類（§5.2）。** S→T 切換的向下不連續跳躍被 FP-detector 當對角穿越誤標 stable；真實長期是週期 ~6 的 limit cycle（n=401…4001 一致），實現均值比報的 s* 低 1–2 pts。涉及「pinned at switch」的結論須用直接 simulate 驗證。
- **雙穩態的 stable/unstable 形式標籤 grid-fragile，但雙穩態本身真實（§2.2, §4.2）。** 以 basin / trajectory 測試（獨立於 FP-detector）確認 saddle-node 真實存在；只有形式標籤與精確 crossing 點（抖 ~0.006）對網格敏感。
- **真正的網格穩健結論**（門檻、s*、inc_adv、cell inventory）已在 n=101/201/401/801/1601（部分到 2001/4001）逐一確認一致，非網格假象。

### 8.2 度量陷阱（metric traps）

- **`incumbent_adv` 是 ratio `(disc_hi−disc_lo)/|disc_lo|`，分母會發散。** §1.1、§3.4、§5.4 三個「驚人」結果本質都是分母在動（耐心放大生命週期、低 p_S 壓縮 base），**不是戰略情境改變**。報告與簡報凡涉在位優勢，**務必同時揭示絕對 gap 或 inc_adv 水平本身**。
- **`analyze` 的 inc_adv / disc_profit 用寫死的 25 期窗口。** 部分幅度（如 §5.4 的 0.45、§1.2 的暫態）對 horizon 敏感（T=200 時 §5.4 降到 ~0.28），定性 pattern 在各 horizon 成立，但**絕對水平不可當無限期值引用**。value function 本身是無限期的，只有報告的 inc_adv 是 T=25。

### 8.3 模型結構限制

- **單一 (1−s) 飽和使多重穩態不易出現。** 增強模型的 shipped 轉移 `s'=ρs+α(1−s)` 在 default 校準下運動律是**凹的**，對任何 κ_ρ 都只有單一穩態。多重穩態 / saddle-node **不是網路效應的免費後果**——只在刻意低留存（ρ_0~0.3）+ 高 κ_ρ 才出現（§2.2）。舊版 `04_extension1.py` 的多重穩態來自不同的 (1−s)² 飽和形式，**不可把其性質誤植到此 harness**（§3.2 的修正即指出此誤植）。
- **三支腳本與 harness 的飽和項形式不一致。** baseline `03_solve_model.py` 用 (1−s)²、`05_competitor_fixed.py` 用單一 (1−s)、`experiments.py`/`06` 用單一 (1−s)+Bass α。本報告所有數字出自 **`experiments.py`（增強模型）**；跨腳本比較須注意此差異（見 `CLAUDE.md` 架構說明）。
- **dimension separation 在動態 DP 不乾淨（§4.1）。** 「revenue 依 P、cost 依 C」的靜態分離只對侷限在遠離邊界 regime 的參數（p_T、φ1）成立；觸及高 s SVOD regime 的參數（p_S, c_A, c_R）都經共用 value function 跨維洩漏。論文不應沿用靜態完美分離假設。
- **若干結果是 regime-existence、非校準預測。** §5.1 的 φ1≳3.55（calibrated φ1=0.5 的 ~7 倍）、§4 部分極端 c_A/c_R 屬「在某參數 regime 存在某現象」的存在性結果，不是對 baseline 校準下廠商行為的預測。引用時須標明參數遠離校準範圍。
- **強假設依賴需標註。** §1.3 的 TVOD 虧本獲客窗口完全靠 `t̃_T>t̃_S`（t̃_T=0 即消失）；§4.4 在位者 c_A-免疫靠「s=0.65 落在 SR cell」的校準幾何（c_R、δ_A、ρ_0 鄰近擾動即破）。這些是強假設驅動的結果，非穩健的一般現象。

---

*報告基於 `experiments.py` harness 的對抗式驗證重跑。confirmed = 數字與機制皆通過驗證；nuanced = 主結論成立但部分數字/機制/framing 需修正（修正已內聯標註 ⚠️）。*
